classes:
- cluster.{{ cookiecutter.cluster_name }}.cicd.worker
- cluster.{{ cookiecutter.cluster_name }}.cicd.control
parameters:
  _param:
    cicd_aio_node_address: ${_param:single_address}
  linux:
    network:
      host:
        cid01:
          address: ${_param:cicd_aio_node_address}
          names:
          - ${_param:cicd_control_node01_hostname}
          - ${_param:cicd_control_node01_hostname}.${_param:cluster_domain}
